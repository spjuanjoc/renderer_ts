{
  "name": "renderer_ts",
  "version": "1.0.0",
  "description": "npm init npm install --save-dev electron tsc --init",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ./dist/index.js",
    "scss": "node-sass — watch scss -o css"
  },
  "author": "juan.castellanos",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8.2.2"
  },
  "dependencies": {
    "node-sass": "^4.13.1"
  }
}
